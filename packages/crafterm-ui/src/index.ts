// @crafterm/ui — reusable, app-agnostic UI component library.
// Components are added here in Phase 1 (see docs/crafterm-ui-inventory.md).
// Contract: each component is a factory `createX(opts) => Handle` that owns its
// DOM and cleanup, with no state/IPC/business-logic imports.
export {}
