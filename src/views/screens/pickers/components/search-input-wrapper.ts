// The pickers' "contains" search box is the @views search-box port —
// re-exported under the legacy name so the per-picker modules consume one impl.
export { createSearchBox as searchInputWrapper } from '../lib/search-box'
