// The settings screen entry (settings.state.ts / settings.ts) has no
// module-local types of its own; shared settings sub-tab types live in
// shared.types.ts. This module exists for structural symmetry.
export {}
