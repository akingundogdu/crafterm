// macOS uses this for the app menu / notification name and the packaged bundle
// name. Shared between the lifecycle entry (index.ts) and the IPC handlers that
// reference it (e.g. the self-update swap).
export const APP_NAME = 'Crafterm'
