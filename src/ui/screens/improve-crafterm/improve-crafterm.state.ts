// Migrated to gea — the implementation now lives in src/views (self-contained,
// §2.7). This legacy module is a thin re-export shim so un-migrated @ui consumers
// keep their imports unchanged until the orchestrator repoints them.
export * from '@views/screens/improve-crafterm/improve-crafterm.state'
