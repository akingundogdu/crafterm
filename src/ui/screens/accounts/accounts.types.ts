// The accounts screen has no module-local types; it operates on the global
// AccountEntry / AccountField shapes. Re-export them for structural symmetry.
export type { AccountEntry, AccountField } from '@ui/types/types'
