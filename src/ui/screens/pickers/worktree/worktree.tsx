// Migrated to the gea @views tree. This legacy entry is a thin re-export
// shim so the shell/spotlight imports keep resolving during migration.
export * from '@views/screens/pickers/worktree/worktree'
