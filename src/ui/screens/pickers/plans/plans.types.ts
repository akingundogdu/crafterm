// The plans picker has no module-local types; its helpers are generic over the
// plan shape (`{ name: string; path: string }`). This module exists for
// structural symmetry with the other pickers.
export {}
